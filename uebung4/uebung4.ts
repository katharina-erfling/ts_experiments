// Verdoppelt den übergebenen Zahlenwert und gibt ihn zurück
function processData(input: number): number {
    return input * 2;
}

// Ruft processData mit 100 auf → result = 200
let result = processData(100);
console.log(result); // Gibt 200 in der Konsole aus